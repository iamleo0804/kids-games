> ⚠️ **開發規範**：在修改 UI 或新增功能時，請務必參考 `.antigravity/docs/design_system.md`。嚴格遵守 HSL 色彩定義，並確保所有新按鈕皆繼承 `GameButton` 的樣式與交互邏輯。

## 🛠️ 環境配置

- **Shell**: 使用 `zsh` 執行指令。
- **Node.js**: 使用 `nvm` 管理版本，預設應為 `v20`。
- **指令範例**: `zsh -c "source ~/.nvm/nvm.sh && nvm use 20 && <command>"`
